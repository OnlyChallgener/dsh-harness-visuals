#!/bin/sh
set -eu
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PNPM_ENTRY="$SCRIPT_DIR/../node_modules/pnpm/bin/pnpm.cjs"
if [ -n "${DSH_DESKTOP_NODE:-}" ]; then
  exec "$DSH_DESKTOP_NODE" "$PNPM_ENTRY" "$@"
fi
exec node "$PNPM_ENTRY" "$@"
